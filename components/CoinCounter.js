import React, { useState } from 'react';
import { StyleSheet, Text, TextInput, View } from 'react-native';

export default function CoinCounter() {
    return(
        <View>
            <Text>This is a coin counter card</Text>
        </View>
    )
}